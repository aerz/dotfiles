#!/usr/bin/env bash

sig_custom_updates() {
    # pkill -SIGRTMIN+8 waybar
    systemctl --user kill --signal="SIGRTMIN+8" waybar-main.service
}

case "$1" in
    *)
        sig_custom_updates
        /usr/bin/paru "$@"
        ;;
esac
