#!/usr/bin/env bash

set -euo pipefail

export DOTFILES_PATH="$HOME/dotfiles"

echo
echo "Installing dotfiles to default path: ${DOTFILES_PATH}"
echo

git --version
git clone git@github.com:aerz/dotfiles.git "$DOTFILES_PATH"

"${DOTFILES_PATH}/bin/dot" self install
