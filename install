#!/usr/bin/env bash

set -euo pipefail

echo
echo "Installing dotfiles to default path: ${HOME}/dotfiles"
echo

git --version
git clone git@github.com:aerz/dotfiles.git "${HOME}/dotfiles"

"${HOME}/dotfiles/bin/dot" self install
