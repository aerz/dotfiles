[user]
	name = Agustin Cisneros
	email = agustincc2@gmail.com
[github]
	user = aerz
[core]
	excludesfile = ~/.gitignore_global
	autocrlf = input
[alias]
	assume = update-index --assume-unchanged
	unassume = update-index --no-assume-unchanged

	undo = reset --soft HEAD~1
	# local changes
	amend = commit -a --amend
	st = status
